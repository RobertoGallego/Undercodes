import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    text: {
        // backgroundColor: "red",
        color: "blue",
    },
    text2: {
        color: "red"
    }
});

export default useStyles;